<V47_DIRECTION_BLIND_BOUNDED_MEMBERSHIP_DECISION>
Return the exact subset of setAOnlyBlockIds plus setBOnlyBlockIds that belongs in the final answer. It may equal Set A's difference membership, Set B's difference membership, or a strict subset between them when one challenge bundle combines valid and invalid changes.
included_difference_block_ids are final retained source memberships, not toggle actions. IDs common to both sets remain fixed and must not be repeated. Never include an ID outside setAOnlyBlockIds plus setBOnlyBlockIds, and do not infer provenance from label order.
First reconstruct the source-correct complete answer under the frozen final_check, then compare both supplied sets to that reconstruction. Do not classify one delta block in isolation.
The immutable source is already readable parsed text. Make the semantic decision; there is no degraded or abstain verdict.
For any atomic block present in only one set, inspect its complete text. If that indivisible block contains any valid target evaluation material, a set that retains the atomic block may be correct despite mixed ordinary content. Separately addressable post-evaluation siblings and independent fact tables do not receive this protection.
When literal inspection evidence is present, inspect every returned excerpt before deciding. It is a mechanically searched attention aid, not an oracle; zero hits and omitted excerpts are not proof that a block lacks target material.
SOURCE_OCCURRENCE_BURDEN: extraction is by authored source address, not by unique semantic information. Before selecting a set that removes an atomic difference block, prove that the complete block contains zero actual valid target material. The same or cleaner target content appearing in another block is never a removal reason. A cross-reference may be dropped only when it contains no actual criteria itself.
FLATTENED_OWNER: a physical parser block can contain multiple internal authored owners. Inspect embedded row labels, cells, and subsection text; an outer ordinary chapter/table label cannot override a later embedded evaluation subcontainer. If valid embedded target material is physically indivisible from the block, retain that source ID.
MULTISTAGE_PREAWARD: screening, shortlisting, and later candidate-selection stages are all pre-award while they still evaluate or compare candidates using technical/service criteria. The positive boundary begins at result/award notification or later lifecycle content, not merely because an earlier evaluation stage ended.
For a local edge difference, reconstruct the uninterrupted authored peer chain. A peer can share an established local negative-result or evaluation effect without repeating a score or evaluator verb; numbering gaps and terse wording are not boundaries. Positive owner or lifecycle boundaries still stop inheritance.
Apply these decisive gates in order before submitting. GATE_1_OWNER: a response-scheme or drafting list controlled only by supplier actions and positive ideals such as complete, reasonable, or targeted has no evaluator; multiple technical directions alone do not make it evaluation source, and an empty set can be correct. GATE_2_LIFECYCLE: once ranking, winner determination, or evaluation is complete, award notification, contract discussion/signature, and legal effect are outside even when numbering continues in the same section; lifecycle outranks sequence continuity. GATE_3_LOCAL_GROUP: when peer named technical/service objects already contain direct negative results, grades, deductions, comparison, pass/fail, or qualitative outcomes, an uninterrupted immediate edge peer shares the established local evaluator unless a positive boundary intervenes; apply this symmetrically regardless of which set contains the edge. GATE_4_ATOMIC: an indivisible paragraph or table containing any valid target evaluation material cannot be partially trimmed, deduplicated against another occurrence, or removed as a whole, so the retaining set wins. GATE_5_SEPARABLE_TAIL: only after establishing that the complete atomic block contains zero valid target material may an independently addressable fact table or new non-evaluation unit after a completed evaluation container be removed.
Submit only included_difference_block_ids and 1 to 24 decisive evidence_block_ids. No verdict label, narrative, or per-block ledger.
</V47_DIRECTION_BLIND_BOUNDED_MEMBERSHIP_DECISION>
