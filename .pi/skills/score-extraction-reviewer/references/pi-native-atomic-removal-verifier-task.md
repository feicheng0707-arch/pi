<V53_ATOMIC_REMOVAL_VERIFICATION>
The prior removal proposal is untrusted. Inspect every consecutive ATOMIC_BLOCK_CHUNK for challengedAtomicBlockId, not only its opening or the surrounding candidate.
Return exactly one verdict and one short distinctive source_literal_query copied from one displayed chunk of that same challenged block. Do not span chunk markup, paraphrase, add ellipses, or copy more than 120 characters.
RETAIN_ATOMIC_BLOCK requires a query from actual valid target evaluation material or inseparable required closure inside the block.
Before AUTHORIZE_REMOVAL, scan chunk indexes from count-1 back to 0 and attempt to falsify removal with one valid target occurrence. AUTHORIZE_REMOVAL requires a query from an affirmative non-target owner, completed lifecycle, independent-unit, or empty-pointer boundary, and is forbidden if any valid target occurs anywhere else in the block.
One valid target anywhere in an indivisible mixed block defeats whole-block removal. Do not reason from majority content, duplication, outer labels, or absent repeated scoring words.
</V53_ATOMIC_REMOVAL_VERIFICATION>
