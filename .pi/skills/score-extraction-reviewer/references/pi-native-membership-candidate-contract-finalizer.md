# Blind Membership Challenge Finalizer

The Reviewer claim and evidence leads are withheld. Use the complete immutable source and frozen single-prompt contract to adjudicate only Runtime-derived `additionChallengeBlockIds` and `removalChallengeBlockIds`. Candidate history and challenge direction have no source weight.

Always return `publish` when source is readable and sufficient. `publish` may approve all, some, or none of the challenged blocks; both approval arrays may be empty. Use `degraded` exclusively when immutable source is missing, truncated, illegible, internally unresolvable, or otherwise prevents reliable judgment. Never use `degraded` merely to express caution after reaching a supported semantic conclusion.

For additions, require positive current-project pre-award target membership or indispensable textual closure. At a candidate edge, an uninterrupted peer can share an established local evaluation effect without repeating a score or evaluator verb. Defective, incomplete, weak, unavailable, not provided, zero score, or failed can be terminal results. Numbering jumps and missing intermediate visible items are not boundaries.

For removals, require an affirmative new owner, lifecycle, peer section, independent content unit, or container-end boundary. Short wording, missing repeated score language, technical relevance, or adjacency is insufficient. If removals cover the whole candidate, test both valid evaluator paths before approving: peer technical or service items with direct negative outcomes/grades/deductions/pass-fail/qualitative results can establish a local evaluator; two or more distinct named capabilities, properties, configurations, plans, commitments, or response attributes linked to comprehensive evaluation, comparison, separate scoring, ranking, accumulated score, or selection establish a relational evaluator even without itemized points. Supplier response headings, drafting actions, ordinary requirements, and positive ideals alone are not evaluation anchors.

Result notification, award notice, contract discussion, signature, legal effect, acceptance, service-period assessment, payment deduction, monetary penalty, and later performance are not chronological closure. An independent fact table after completed scoring does not inherit a nearby evaluator.

Use canonical addresses literally: numeric ID `N` maps only to `段落N`, and numbering printed inside source text is never an address. Approval ranges must be exact subsets of the corresponding Runtime challenge IDs.
