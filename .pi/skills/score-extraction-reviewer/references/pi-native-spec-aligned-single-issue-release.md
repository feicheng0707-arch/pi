# Adversarial Regression Release

An exact change has passed the Primary but is not published. Assume the override may be a regression. Independently search the complete source for the strongest counterexample that would preserve the frozen candidate.

Decision mapping is literal:

- `approve_change` publishes the exact proposed `include` or `exclude` operation;
- `reject_change` vetoes it and preserves the candidate;
- `degraded` preserves the candidate because reliable release proof is unavailable.

For an `include`, try to prove that any issue block has a forbidden owner or lifecycle, is only related background or evidence material, or is separable from the actual evaluation container. Publish only if every block survives that attack and positively satisfies target membership or indispensable closure.

For an immediate edge `include`, test the strongest ordinary-requirement interpretation, then test the competing shared-group interpretation. A peer technical or service factor may inherit an evaluator-owned effect from an uninterrupted selected list with multiple explicit negative results or other terminal outcomes when numbering, grammar, and local object remain continuous and no affirmative boundary intervenes. Do not veto solely because the edge block omits a repeated score verb.

For an `exclude`, try to prove that any issue block is a genuine scoring/evaluation member, a necessary authored boundary, or part of a local negative-result group whose shared effect does not repeat on every sibling. Publish only if every block survives that attack and is positively non-target.

Before publishing exclusion of a response outline or format, search for an independently established evaluation factor set elsewhere in source. Veto the exclusion when the challenged section systematically mirrors all or the main named factors and explicitly requires the corresponding bidder material; that is evaluator-dependent response mapping. Generic wrappers, signatures, price forms, weak overlap, and unrelated material remain removable.

Do not judge whether the authored evaluation rule is sensible, complete, internally consistent, or well drafted. Do not use prior approval, candidate history, role identity, confidence, edit size, or Reviewer prose as evidence. Do not alter direction or ranges. Cite 1 to 8 decisive source block IDs.
