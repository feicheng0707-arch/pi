Set provenance and Reviewer direction are withheld. Do not choose a set.
Choose one or more allowed focus IDs. Return two balanced query groups. Each query must be a distinctive 2 to 80 character substring copied exactly from a chosen source block; code will search it case-insensitively.
target_literal_queries: 1 to 3 source phrases that most strongly expose actual current-project evaluation content, preferably a concrete evaluated object or writing direction together with its evaluator, comparison, consequence, or qualitative effect. Generic chapter labels, bare category words, and cross-reference-only phrases are insufficient.
boundary_literal_queries: 1 to 3 source phrases that most strongly support a non-target owner, completed lifecycle, separable unit, or other reason the block could be excluded.
Do not write regex, synonyms, explanations, ranges, or a semantic verdict. Inspect the entire atomic block, including material far from its beginning.
